# -*- mode: snippet -*-
# name: graph
# key: graph
# expand-env: ((yas/indent-line 'fixed))
# --
${1:di}graph ${2:graph_id} {$0
    // ID=ID;
    // ex) rankdir=LR;

    // attr_stmt
    graph [];
    node [];
    edge [];

    // subgraph
    // ex) subgraph ID {...}

    // node_stmt
    // ex) node_id [...];

    // edge_stmt
    // ex) In digraph => node_id:port -> node_id:port [...];
    // ex) In graph => node_id:port -- subgraph_id:port [...];
}